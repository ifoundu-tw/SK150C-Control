SK150C Modbus 控制台 - v39 原型

執行檔：
SK150C_Control_v39.exe

v39 更新：
- 修正滿電截止電壓卡在「等待通訊空檔 OFF」的問題。
- OFF 指令不再從 UI 更新瞬間搶 busy，而是掛到輪巡讀取結尾，在同一個 serial 占用期間送出 0012H=0。
- 輪巡不中斷；下一輪輪巡會自然讀回輸出狀態並確認 OFF。
- 恢復 ON 也改為掛到輪巡結尾送出，避免同樣撞到 busy。
- 新增「等待輪巡送 OFF」「等待輪巡恢復 ON」等狀態提示。

v38 更新：
- 滿電截止電壓流程改為不中斷輪巡的狀態機。
- 到達輪詢時間後先進入「要求輸出 OFF」，持續等待通訊空檔送 OFF，不再因忙碌逾時而放棄。
- 由主輪巡確認輸出狀態已 OFF，確認 OFF 後才開始 5 秒穩定等待。
- 5 秒等待結束後使用最新輪巡的輸出電壓判斷是否截止。
- 若電壓未達標，送出恢復 ON；若通訊忙碌或失敗，會維持流程並繼續重試。
- 電壓檢查整段期間仍會暫停截止電流判斷並清除低電流計時。
- 快速輸入補套用到滿電截止電流、滿電截止電壓、輪詢時間三個輸入框。

v37 更新：
- 滿電截止電壓檢查改為嚴謹 OFF 流程：先暫停自動輪詢，再搶通訊權。
- OFF 寫入改為寫入 0012H=0 後讀回 0012H 確認為 0，確認成功才等待 5 秒。
- 若通訊忙碌、OFF 寫入失敗或 OFF 讀回失敗，狀態列會明確顯示失敗原因，不會進入電壓量測。
- 電壓未達標恢復 ON 時，會寫入 0012H=1 並讀回確認為 1。
- TX/RX 紀錄新增截止電壓檢查 SYS 訊息，方便確認 OFF 是否真的送出與讀回。

v36 更新：
- 右欄內容寬度由 250px 調整為 260px，右欄整體向左延伸。
- 常用頁取消捲軸，並壓縮參數列、快捷組與滿電截止區高度。
- 移除「滿電截止」區塊標題。
- 滿電截止電壓與輪詢時間改為同一列：左側「套用電壓 + ON/OFF」，右側「套用」。
- 輪詢時間改為精簡輸入框，移除加減微調按鈕。
- 滿電截止電壓 OFF 後等待穩定時間由 1 秒改為 5 秒。
- 滿電截止電壓檢查期間會暫停滿電截止電流判斷並清除低電流計時，避免 OFF 檢查時誤觸發電流截止。

v35 更新：
- 修正右欄「輸出控制」標題與常用/進階分頁重疊的版面問題。
- 「輸出開關」移到右欄固定底部，與 ON/OFF 按鈕同一行，並固定在「刷新 M0-M10」上方。
- 滿電截止電流、滿電截止電壓改為精簡輸入列，移除 -1、-0.1、+0.1、+1 微調按鈕以節省高度。
- 滿電截止電流/電壓啟用控制改為彩色 ON/OFF 按鈕，放在套用按鈕右側。
- 滿電截止功能仍維持 M0-M10 分組保存、Enter 套用與 dirty 黃底提示。

v34 更新：
- 右欄改為「常用 / 進階」分頁，LVP 與 OPP 移到進階頁。
- 常用頁新增上位機功能「滿電截止」，不寫入從機未知寄存器。
- 新增滿電截止電流：輸出 ON 後延遲 10 秒，輸出電流低於門檻連續 5 秒時自動 OFF。
- 新增滿電截止電壓：依輪詢時間週期性 OFF，等待 1 秒後讀輸出電壓，達到目標電壓則保持 OFF，否則恢復 ON。
- 滿電截止電流/電壓設定跟隨 M0-M10 分組保存。
- 設定保存於 HKCU\Software\SK150C_Control\Presets\M0~M10，不產生額外設定檔。
- 滿電截止狀態列會顯示停用、等待 ON、延遲、低電流計時、電壓檢查與已截止等狀態。

v33 更新：
- 右欄參數輸入框新增 Enter 送出。
- 設定電壓、設定電流、LVP、OVP、OCP、OPP 欄位按 Enter 等同按下對應寫入按鈕。
- 按 Enter 會先提交目前輸入文字，再沿用原本 busy、dirty 與寫入後讀回確認流程。

v32 更新：
- 主 V/I 歷史圖在左側 now 線頭新增大字即時標籤，顯示目前輸出電壓與輸出電流。
- 電壓標籤使用綠色，電流標籤使用黃色，字體接近輸出功率大字。
- 標籤會先計算文字大小，自動避開圖表上下邊界。
- 當 V/I 線頭太接近時，標籤會自動上下錯開，避免互相覆蓋。

v31 更新：
- 主 V/I 歷史圖左右軸交換：左軸改為電流 A，右軸改為電壓 V。
- 底部圖例順序同步調整為 IOUT / VOUT，對應新的左右軸閱讀方式。

v30 更新：
- 主 V/I 圖表時間方向改為左側 now、右側為歷史時間。
- 輸出功率大格內的小圖同步改為左側 now、右側為歷史時間。
- 圖表時間視窗改為連續式動態縮放：依累積時間每次逐步變寬，最低 10 秒、最高 1 小時。
- 時間標籤維持自然格式，自動顯示秒/分鐘，減少階段式縮放造成的跳動感。

v29 更新：
- 長時間運行優化：TX/RX 通訊紀錄自動保留最近 2000 行，避免文字框越跑越慢。
- 自動輪詢新增連續失敗保護：連續 3 次輪詢失敗後暫時降頻到至少 2000ms，成功回包後自動恢復原本間隔。
- 寫入、快捷組等既有流程維持 busy 鎖定，輪詢成功才執行背景保護同步與裝置選項同步。
- UI 數值未變時不重複更新文字與字型，降低長時間輪詢時的重繪負擔。
- 正常狀態不再每秒反覆覆寫，只有異常恢復或保護狀態變化時更新。

v28 更新：
- 中下方參數區改為三類底色：目標參數、運作參數、實際參數。
- 參數排列改為：左欄設定/保護目標，中欄累計/模式/輸出狀態，右側顯示輸出功率、輸出電壓、輸出電流與總狀態。
- 新增「計時器」欄位，使用 000AH/000BH/000CH 顯示開啟時長 HH:MM:SS。
- 保留 V 綠色、A 黃色、W 紅色的數字顏色規則。

v27 更新：
- 上方 V/I 歷史曲線新增垂直時間格線，方便對齊時間點。
- 輸出功率大格內的小圖新增動態時間軸、垂直時間格線與底部時間標籤。
- 輸出功率小圖 Y 軸下限固定 0W，上限自動選 5/10/20/50/100/150W，最大不超過 150W。
- 中下方原「目前調用」大格改為總狀態大格，左欄不再顯示狀態文字。
- 快捷組目前成功調用改由 M0-M10 按鈕反色表示。
- 保護狀態 0010H 依說明書註 3 顯示 0-11 對應名稱，不再只顯示代碼。
- 英文介面同步翻譯保護狀態內容。
- 當保護狀態不是 0 時，保護狀態 tile 與總狀態大格會反紅；讀回 0 後自動恢復。

v26 更新：
- 快捷組 M0-M10 新增成功調用反色提示。
- 只有在該 M 組讀取成功、寫入 001DH 調用成功並同步後，對應按鈕才會切換為深綠反色。
- 其他 M 組會自動恢復一般白底樣式。
- 若調用失敗或忙碌，保留上一個成功調用的反色狀態，不會因單純點擊而誤判。

v25 更新：
- 左欄新增「語言」切換，可選中文 / English。
- 預設語言為中文。
- 切換語言後會即時更新主要 UI 標題、按鈕、參數欄位、狀態 tile 與常用狀態前綴。
- TX/RX 通訊紀錄、M0-M10、版本號、作者與 V/A/W/Ah/Wh 單位不翻譯。
- EXE 外部圖示維持新版去背圖片圖示。

v24 更新：
- EXE 外部圖示改為新版去背圖片圖示，編譯時已嵌入 win32 icon。
- 右欄「輸出控制」標題右側新增「快速輸入」開關。
- 「快速輸入」預設開啟。
- 開啟時，右欄設定電壓、設定電流、LVP、OVP、OCP、OPP 欄位取得焦點/點擊後會自動全選，方便直接覆蓋輸入。
- 快速輸入只全選、不清空，因此單純點擊欄位不會讓欄位進入 dirty 狀態。

v23 更新：
- 「目前調用」從右欄底部移到中間下方狀態區。
- 「目前調用」位於「輸出狀態 / 保護狀態」右側，綠底白字並跨兩格高度。
- 「目前調用」字體加大加粗，作為主要狀態顯示。
- 右欄底部現在只保留「刷新 M0-M10」按鈕。

v22 更新：
- 右欄最下方「目前調用」上方新增「刷新 M0-M10」按鈕。
- 點擊後會暫停自動輪詢，依序讀取 M0-M10 的 V-SET / I-SET。
- 刷新完成後更新快捷組按鈕內的綠色電壓與黃色電流。
- 如果刷新前自動輪詢正在執行，刷新完成後會恢復原本輪詢狀態。

v21 更新：
- 左下角圖片改為新版去背圖片。
- EXE 內嵌主程式圖片同步改為新版，不依賴外部圖片檔。

v20 更新：
- 主圖表 Y 軸改以設定值為基準。
- 左軸電壓預設 0 到目前設定電壓，例如設定 21V 時左軸最高為 21V。
- 右軸電流預設 0 到目前設定電流，例如設定 2A 時右軸最高為 2A。
- 若實際輸出超過設定值，對應 Y 軸會暫時自動放大，避免曲線被頂住或切掉。
- 輪詢讀到設定值、或右欄寫入電壓/電流確認成功後，都會同步更新圖表基準。

v19 更新：
- 主圖表時間軸改成動態視窗，不再一啟動就固定 -60m。
- 剛開始會依資料長度自動使用 10s / 30s / 60s / 2m / 5m / 10m / 15m / 30m / 60m 視窗。
- 跑滿 1 小時後才固定顯示最近 60 分鐘。
- 原始資料與壓縮邏輯維持 v18：原始點超過 1 小時後才壓縮進 3 天分鐘統計。

v18 更新：
- 圖表壓縮邏輯修正為「原始點保留 1 小時，超過 1 小時後才壓縮」。
- 新進資料先只保留在原始區，不再一進資料就同步壓縮。
- 超過 1 小時被移出的 V / A / W 原始點，才合併到分鐘統計資料。
- 右欄「寫入電壓」「寫入電流」只有在寫入後讀回確認一致時，才同步更新下方目前 M 組按鈕的電壓/電流顯示。

v17 更新：
- 主歷史圖表改為最近 1 小時時間窗，X 軸顯示 -60m / -45m / -30m / -15m / now。
- 原始 VOUT / IOUT / POWER 點只保留最近 1 小時，避免長時間輪詢後直接用全部原始點畫圖。
- 背景新增每分鐘壓縮資料：V / A / W 會保存平均、最小、最大，保留最近 3 天。
- 這版先維持主圖表只畫 VOUT 與 IOUT，3 天壓縮資料先作為後續長時間趨勢/統計使用。

v16 更新：
- 快捷組按鈕改為自繪三段式，不改格子大小。
- 按鈕左側顯示 M0-M10，右上顯示綠色電壓，右下顯示黃色電流。
- 快捷組摘要掃描改讀 V-SET 與 I-SET 兩個寄存器，顯示每組電壓與電流。
- 調用某個 M 組讀回成功時，同步更新該按鈕的電壓/電流。
- 右下固定狀態區移除「保護寫入」提示，只保留目前調用。

v15 更新：
- 快捷組電壓摘要改為單行顯示，例如 M0 21V、M1 --V，避免按鈕換行被吃掉。
- 連線成功後先讀取 M0-M10 電壓摘要，再啟動自動輪詢，避免初始化時被輪詢搶通訊。
- 快捷組摘要掃描中狀態列會顯示「讀取快捷組摘要 M0-M10」。

v14 更新：
- POWER 輸出功率倍率依實測改為 raw / 100 W，顯示格式改為 0.00W。
- 黑色頂欄改用固定表格排版，修正最大化啟動時「已連線 COMx 115200 8N1」吃字。
- 連線成功後自動掃描 M0-M10 的 V-SET，快捷組按鈕顯示例如 M0 (21V)。
- 調用某個 M 組並成功讀回資料時，同步更新該顆 M 按鈕上的電壓摘要。

v13 更新：
- 左欄新增裝置資訊：產品型號 MODEL=0016H、固件版本 VERSION=0017H。
- 輪詢間隔由下拉選單改成 100 / 300 / 1000 三個按鈕，預設 1000ms。
- 右欄快捷組取消所有「編」按鈕，M0-M10 改為大按鈕兩欄填滿。
- 右欄 LVP/OVP/OCP/OPP 寫入改為寫入目前調用中的 M 組。
- 右欄主要參數 dirty 狀態改為確認制：黃色為已修改未確認，紅色為寫入/讀回確認中，只有寫入成功且讀回一致才恢復正常。
- IOUT 輸出電流倍率依實測改為 raw / 1000 A。
- 主歷史圖表只顯示 VOUT 與 IOUT，兩條曲線各自獨立自動縮放。
- 輸出功率移到右側大功率格內，以大字加紅色迷你趨勢圖顯示。

v12 更新：
- 右欄設定電壓、設定電流、LVP、OVP、OCP、OPP 增加 dirty 狀態。
- 使用者按 +/- 或手動輸入時，該欄位整塊背景會變黃提示「尚未寫入/重新載入」。
- 欄位 dirty 時，自動輪詢與目前快捷組同步不會覆蓋正在調整的值。
- 按下對應「寫入」且可送出時會解除黃色提示。
- 按 M0-M10 或「編」重新讀取快捷組時，會強制載入該組參數並解除黃色提示。

v11 更新：
- 設定電流 I-SET 倍率改為 raw / 1000 A。
- 設定電流寫入改為 A * 1000。
- 快捷組 I-SET 反饋也改為 raw / 1000 A。
- LVP 暫時維持原本 offset 與倍率，等待實測 RX 確認。

v10 更新：
- 重新定義快捷組主按鈕與編輯鍵行為。
- 按 M0-M10 主按鈕時：
  1. 先讀取該 M 組參數。
  2. 將 V-SET、I-SET、LVP、OVP、OCP、OPP 反饋到右欄欄位。
  3. 再送出 001DH = M# 調用到從機。
  4. 狀態顯示目前調用：M#。
- 按 M0編-M10編時：
  1. 只讀取該 M 組參數。
  2. 將參數反饋到右欄欄位。
  3. 不送出 001DH，不調用從機。
  4. 狀態顯示正在編輯：M#。

v9 更新：
- 右欄最下方新增固定狀態區，不會跟著右欄捲動。
- 狀態區分成兩行醒目顯示：
  目前調用：M#
  正在編輯：M#
- 「目前調用」與「正在編輯」使用不同底色、大字粗體。
- 按 M0-M10 主按鈕時會立即顯示調用中、成功或失敗。
- 按「編」時會立即顯示讀取中、讀取成功或讀取失敗。
- 忙碌時不再無聲忽略，會在對應狀態條顯示忙碌中。

v8 更新：
- 程式啟動時視窗直接最大化。
- 左欄下方新增圖片、版本號與作者簽名。
- 圖片已內嵌進 EXE，不依賴桌面原始圖片路徑。
- 版本顯示為 SK150C_Control_v8。
- 作者簽名顯示為「作者：鄭large」。
- 右欄參數區改為區塊式排版，修正設定電壓/設定電流之間空隙過大問題。
- 修正 LVP 寫入按鈕覆蓋步進按鈕的排版問題。
- 快捷組改為兩欄直排：左欄 M0-M5，右欄 M6-M10。
- 熄屏時間維持原本行為，暫時不改。

v7 更新：
- 中央下方參數改為單行式，標題右邊直接接數值，不再上下換行。
- 套用固定顏色規則：電壓 V 為綠色、電流 A 為黃色、功率 W 為紅色。
- 歷史曲線顏色同步改為 V 綠、A 黃、W 紅。
- 右欄參數輸入框與單位顏色同步套用 V/A/W 規則。
- 右欄快捷組改為每組兩個按鈕：M0-M10 主按鈕直接調用，旁邊「編」按鈕才進入編輯。
- 快捷組編輯模式會讀回該 M 組的 V-SET、I-SET、LVP、OVP、OCP、OPP。
- 在「編輯：M#」模式下，寫入電壓/電流/LVP/OVP/OCP/OPP 會寫到該 M 組資料區。
- 未進入快捷組編輯時，電壓/電流寫入仍是即時輸出設定；LVP/OVP/OCP/OPP 會提示先選擇編輯組。
- 右欄項目增加後改為可捲動，避免壓縮吃字。

v6 更新：
- 波特率預設改為 115200。
- 程式啟動時 COM 埠預設選擇最大數字，例如 COM12 優先於 COM7。
- 程式啟動後會自動嘗試連線一次，失敗時不跳錯誤視窗，等待使用者手動操作。
- 中央下方參數格縮小一般字體，並加入自動縮字，避免吃字。
- 容量/能量標題縮短為「容量 AH」「能量 WH」。
- 右欄調參按鈕文字縮短，例如 +1.000 改為 +1、+0.100 改為 +0.1。
- 右欄調參區重新分配按鈕、輸入框、單位寬度，降低吃字機率。

v5 更新：
- 右欄設定電壓、設定電流、OVP、OCP 改成大按鍵步進調整。
- 加減按鈕只改介面數值，必須按「寫入」才會送到設備。
- OCP 維持實測倍率：寫入 A * 1000，讀回 raw / 1000。
- 中央即時參數重新排列，輸出功率改為右側大格顯示。
- 中央新增系統溫度、輸出模式 CV/CC、輸出狀態 ON/OFF。
- 中央新增輸出容量 AH、輸出能量 WH、保護狀態。
- 左欄新增背光亮度、熄屏時間、蜂鳴音、按鍵鎖。
- 左欄新增重啟、恢復出廠設定、校準/清零，按下後會先跳確認視窗。
- 自動輪詢讀取 0000H~0012H，用來同步設定值、輸出值、狀態與累積量。
- 背光、熄屏、蜂鳴音每幾輪同步一次，減少通訊負擔。
- 寫入命令會短暫等待輪詢空檔，降低按下後剛好忙碌而沒送出的機率。

已保留功能：
- COM 埠、波特率、站號選擇。
- Modbus RTU CRC16，低位在前。
- 0x03 讀取、0x06 單暫存器寫入。
- 每次請求最多重試 3 次。
- 底部顯示 TX/RX 原始 hex log。
- 左側統計成功、失敗、CRC 錯誤、重試次數。
- 快捷組 M0-M10 調用後會回讀設定電壓、設定電流、OVP、OCP。

目前寄存器假設：
- 0000H V-SET：raw / 100 V
- 0001H I-SET：raw / 1000 A
- 0002H VOUT：raw / 100 V
- 0003H IOUT：raw / 1000 A
- 0004H POWER：raw / 100 W
- 0006H/0007H AH：32-bit mAh，顯示 Ah
- 0008H/0009H WH：32-bit mWh，顯示 Wh
- 000DH 系統溫度：raw / 10 C
- 000FH 按鍵鎖：0/1
- 0010H 保護狀態
- 0011H CV/CC：0=CV，1=CC
- 0012H 輸出開關：0=OFF，1=ON
- 0014H 背光亮度：0-5
- 0015H 熄屏時間：分鐘
- 001CH 蜂鳴音：0/1
- 001DH 快捷組：M0-M10
- 0020H 恢復出廠設定
- 0021H 校準/清零
- 002FH 重啟
- 0053H + M*0010H OVP：raw / 100 V
- 0054H + M*0010H OCP：raw / 1000 A

注意：
這版仍是原型，建議先用較低電壓電流、小負載測試。若真機顯示與介面倍率不同，再依實測修正。
